# Optional cloud storage dependencies
# Install only if deploying to cloud with S3/GCS support

# AWS S3 support
boto3>=1.34.0

# Google Cloud Storage support
google-cloud-storage>=2.14.0

# Streamlit for dashboard (required in cloud deployment)
streamlit>=1.30.0
