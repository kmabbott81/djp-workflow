# Core dependencies for djp-workflow
# Use pip-compile to generate requirements.txt: pip-compile requirements.in

# Note: agents package from OpenAI - not on PyPI, install separately:
# pip install git+https://github.com/openai/swarm.git
# CI Stabilization: Cap pandas to support Python 3.9+ (pandas 2.2+ requires Python >=3.11)
pandas>=2.0.0,<2.2.0
ujson>=5.0.0
jsonschema>=4.0.0
pydantic>=2.0.0
httpx>=0.27.0  # Sprint 49 Phase B: Actions webhook adapter

# Sprint 51 Phase 1: Database + Auth
asyncpg>=0.30.0         # Async Postgres driver
alembic>=1.13.0         # Database migrations
sqlalchemy>=2.0.30      # ORM
argon2-cffi>=23.1.0     # Secure password hashing
psycopg2-binary>=2.9.9  # Postgres adapter for Alembic

# Sprint 51 Phase 2: Rate limiting
redis>=5.0.0            # Redis client for durable rate limiting

# Sprint 55 Week 3: AI agent infrastructure
openai>=1.0.0           # OpenAI API for natural language action planning
